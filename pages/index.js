// import { useAuth } from '../utils/context/authContext';

function Home() {
  // const { user } = useAuth();

  return (
    <div
      className="home"
    >
      <h1>HOME</h1>
    </div>
  );
}

export default Home;
