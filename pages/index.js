// import { useAuth } from '../utils/context/authContext';

function Home() {
  // const { user } = useAuth();

  return (
    <div
      className="home"
    >
      <h1>Welcome to SESH!</h1>
    </div>
  );
}

export default Home;
